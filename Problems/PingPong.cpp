#include <bits/stdc++.h>
using namespace std;
int t;
int main(){
    cin >> t;
    while (t--){
        int x, y;
        scanf("%d %d", &x, &y);
        printf("%d %d\n", x-1, y);
    }
    return 0;
}